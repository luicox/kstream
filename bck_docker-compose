version: '3.8'

services:
  kstream-app-1:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: kstream-app-1

  kstream-app-2:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: kstream-app-2

  kstream-app-3:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: kstream-app-3
